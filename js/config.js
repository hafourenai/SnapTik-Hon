const APIConfig = (() => {
    const getConfig = () => {
        return {
            appName: 'HAFOURENAI',
            version: '3.0',
            method: 'rapidapi'
        };
    };

    return { getConfig };
})();